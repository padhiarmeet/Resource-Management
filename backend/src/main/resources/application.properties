spring.application.name=resource_management

# Server port configuration for cloud deployment
server.port=${PORT:8080}

# for activating the specific profile
# spring.profiles.active=dev

# security ||
spring.security.user.name=meet
spring.security.user.password=padhiar

# postgresDatabase ||
#ostgresql://postgres:1234@localhost/postgres
spring.datasource.url=jdbc:postgresql://ep-red-meadow-adk8qh3w-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_qJdL0eXEKnR7

spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

# CORS allowed origins (comma-separated)
app.cors.allowed-origins=http://localhost:3000,https://resource-management-g3gd-git-main-padhiarmeets-projects.vercel.app,https://resource-management-g3gd.vercel.app
logging.level.org.springframework.security=DEBUG

management.endpoints.web.exposure.include=health,info,metrics

# JWT Configuration
JWT_SECRET=meetpadhiar1234567890meetpadhiar1234567890meetpadhiar1234567890your_super_secret_jwt_key_here_min_32_chars
JWT_ISSUER=resource_management
JWT_ACCESS_TTL_SECONDS=3600
JWT_REFRESH_TTL_SECONDS=86400
JWT_REFRESH_COOKIE_NAME=refreshToken
JWT_COOKIE_SECURE=false
JWT_COOKIE_HTTP_ONLY=true
JWT_COOKIE_SAME_SITE=lax

//production


security.jwt.secret=${JWT_SECRET:meetpadhiar}
security.jwt.issuer=${JWT_ISSUER:resource_management}
security.jwt.access-ttl-seconds=${JWT_ACCESS_TTL_SECONDS:3600}
security.jwt.refresh-ttl-seconds=${JWT_REFRESH_TTL_SECONDS:86400}
security.jwt.refresh-token-cookie-name=${JWT_REFRESH_COOKIE_NAME:refreshToken}
security.jwt.cookie-secure=${JWT_COOKIE_SECURE:true}
security.jwt.cookie-http-only=${JWT_COOKIE_HTTP_ONLY:true}
security.jwt.cookie-same-site=${JWT_COOKIE_SAME_SITE:lax}
security.jwt.cookie-domain=${JWT_COOKIE_DOMAIN:localhost}
